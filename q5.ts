const famousperson: string = " Albert Einstein ";

const message: string = `${famousperson} once a said, “A person who never made a mistake never tried anything new.” `;

console.log(message);

